{
  "name": "@mycash/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "wrangler d1 migrations apply mycash-dev --local",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mycash/database": "workspace:*",
    "@mycash/email": "workspace:*",
    "drizzle-orm": "^0.39.3",
    "hashids": "^2.3.0",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "@vitest/coverage-v8": "^4.1.8",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8",
    "wrangler": "^3.114.0"
  }
}
